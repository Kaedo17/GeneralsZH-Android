name: Build Android APK

permissions:
  contents: read

on:
  workflow_dispatch:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build-android:
    name: Android APK (arm64-v8a)
    runs-on: ubuntu-latest
    timeout-minutes: 180

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          accept-android-sdk-licenses: true

      - name: Install Android NDK
        run: |
          sdkmanager "ndk;28.2.13676358"
          echo "ANDROID_NDK_HOME=${ANDROID_HOME}/ndk/28.2.13676358" >> $GITHUB_ENV
          echo "NDK installed: $(ls ${ANDROID_HOME}/ndk/28.2.13676358/)"

      - name: Install build dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq --no-install-recommends \
            cmake ninja-build meson python3-pip pkg-config zip
          pip3 install meson
          echo "Build tools:"
          cmake --version
          meson --version
          ninja --version

      - name: Apply DXVK Android patch
        run: |
          cd references/fbraz3-dxvk
          git apply --check "${GITHUB_WORKSPACE}/Patches/dxvk-android.patch" 2>/dev/null || \
            git apply "${GITHUB_WORKSPACE}/Patches/dxvk-android.patch"
          echo "DXVK Android patch applied"

      - name: Cache DXVK build
        uses: actions/cache@v4
        with:
          path: android/app/.cxx
          key: dxvk-cxx-${{ runner.os }}-${{ hashFiles('references/fbraz3-dxvk/**', 'CMakeLists.txt', 'cmake/**') }}
          restore-keys: dxvk-cxx-${{ runner.os }}-

      - name: Generate Gradle wrapper
        working-directory: android
        run: |
          gradle wrapper --gradle-version 8.7
          chmod +x gradlew

      - name: Build APK (assembleRelease) — pass 1
        working-directory: android
        env:
          JAVA_HOME: ${{ env.JAVA_HOME }}
          GRADLE_OPTS: "-Xmx8g -XX:+UseG1GC -XX:MaxMetaspaceSize=512m"
        run: |
          ./gradlew assembleRelease --no-daemon --stacktrace 2>&1 | tee ${GITHUB_WORKSPACE}/build_android.log || true

      - name: Build DXVK via meson (after pass 1, SDL3 now built)
        run: |
          echo "==> Building DXVK d3d8/d3d9 via meson"
          CXX_BASE=$(find android/app/.cxx -maxdepth 2 -type d -name "RelWithDebInfo" 2>/dev/null | head -1)
          HASH_DIR=$(find "$CXX_BASE" -maxdepth 1 -type d ! -name "RelWithDebInfo" | head -1)
          CXX_DIR="$HASH_DIR/arm64-v8a"
          echo "CXX dir: $CXX_DIR"
          ninja -C "$CXX_DIR" 2>&1 | tail -20 || true
          for lib in libdxvk_d3d8.so libdxvk_d3d9.so; do
            found=$(find "$CXX_DIR" -name "$lib" -type f 2>/dev/null | head -1)
            if [ -n "$found" ]; then
              echo "  DXVK $lib: $found ($(stat -c%s "$found") bytes)"
            else
              echo "  WARNING: $lib not found"
            fi
          done

      - name: Stage ALL .so to jniLibs and rebuild
        working-directory: android
        env:
          JAVA_HOME: ${{ env.JAVA_HOME }}
          GRADLE_OPTS: "-Xmx8g -XX:+UseG1GC -XX:MaxMetaspaceSize=512m"
        run: |
          JNI_LIBS="app/src/main/jniLibs/arm64-v8a"
          mkdir -p "$JNI_LIBS"

          CXX_BASE=$(find app/.cxx -maxdepth 2 -type d -name "RelWithDebInfo" 2>/dev/null | head -1)
          HASH_DIR=$(find "$CXX_BASE" -maxdepth 1 -type d ! -name "RelWithDebInfo" | head -1)
          CXX_DIR="$HASH_DIR/arm64-v8a"

          # Copy ALL .so files from build tree to jniLibs
          for lib in libdxvk_d3d8.so libdxvk_d3d9.so libSDL3.so libSDL3_image.so libopenal.so libfreetype.so libglm.so libgamespy.so libmain.so libmain_generals.so; do
            found=$(find "$CXX_DIR" -maxdepth 5 -name "$lib" -type f 2>/dev/null | head -1)
            if [ -n "$found" ]; then
              cp -f "$found" "$JNI_LIBS/$lib"
              echo "  staged $lib ($(stat -c%s "$JNI_LIBS/$lib") bytes)"
            fi
          done
          echo "=== jniLibs ==="
          ls -lh "$JNI_LIBS/"

          # Full rebuild — forces Gradle to re-package with jniLibs
          ./gradlew assembleRelease --no-daemon --rerun-tasks --stacktrace 2>&1 | tee -a ${GITHUB_WORKSPACE}/build_android.log

      - name: Find APK
        id: find-apk
        run: |
          APK=$(find android/app/build/outputs -name "*.apk" -type f 2>/dev/null | head -1)
          if [ -z "$APK" ]; then
            echo "APK not found"
            find android/app/build/outputs -type f 2>/dev/null || echo "No outputs"
            tail -100 build_android.log 2>/dev/null || true
            exit 1
          fi
          echo "APK found: $APK"
          echo "apk_path=$APK" >> $GITHUB_OUTPUT
          ls -lh "$APK"

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: GeneralsZH-Android-arm64
          path: ${{ steps.find-apk.outputs.apk_path }}
          retention-days: 30

      - name: Upload Build Log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-android
          path: build_android.log
          retention-days: 7
